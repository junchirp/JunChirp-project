import { GoogleCallbackGuard } from './google-callback.guard';

describe('GoogleCallbackGuard', () => {
  it('should be defined', () => {
    expect(new GoogleCallbackGuard()).toBeDefined();
  });
});
