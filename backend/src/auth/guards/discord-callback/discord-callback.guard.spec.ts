import { DiscordCallbackGuard } from './discord-callback.guard';

describe('DiscordCallbackGuard', () => {
  it('should be defined', () => {
    expect(new DiscordCallbackGuard()).toBeDefined();
  });
});
